<%= form_with(model: @registration_token, url: register_path, local: true) do |form| %>
  <h2>Cadastro</h2>
  <div class="field">
    <%= form.label :email %>
    <%= form.email_field :email %>
  </div>

  <div class="actions">
    <%= form.submit 'Solicitar registro' %>
  </div>
<% end %>